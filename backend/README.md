# Backend

FastAPI backend application for the Project Management MVP.

## Setup
Managed with uv:
```bash
uv sync --all-extras
```

## Run
```bash
uv run uvicorn main:app --reload --port 8000
```

## Tests
```bash
uv run pytest
```

